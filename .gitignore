.venv/
__pycache__/
*.pyc
# generated story images (one .png per approved draft; not code, not for github)
data/images/

# pooled drafts / copy, and the live sqlite db
data/*.sqlite

# runtime logs (no secrets, but not code)
data/*.log

# internal agent scratch notes (not for the repo)
RESUME.md

# local secrets / env (NEVER commit tokens)
.env
.env.*
